export interface User {
    id?: string
    email: string
    userName: string
    firstName: string
    lastName: string
    updated_at: Date
}

export interface Profile {
    id?: string
    email: string
    userName: string
    firstName: string
    lastName: string
    updated_at: Date
}

