export interface Profile {
    id?: string
    username: string
    website: string
    avatar_url: string
}

export interface User {
    id?: string
    username: string
    website: string
    avatar_url: string
}